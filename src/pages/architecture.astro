---
import BaseLayout from "../layouts/BaseLayout.astro";
import { crates, layers } from "../data/crates";
---

<BaseLayout
  title="架构"
  description="NetbaDB 的语言边界、编译流水线、crate 依赖方向，以及同步嵌入式核心。"
>
  <div class="wrap content-page">
    <p class="kicker">Architecture</p>
    <h1 class="page-title">语言停在前端。<br />引擎只认 IR。</h1>
    <p class="page-deck">
      NetbaDB 把应用语言关在前端边界之外。Go、Rust 或未来的 schema 前端都应产出同一份
      Canonical Schema IR。核心不检查 Go 类型，也不读取应用侧 Rust 结构体。
    </p>

    <div class="stack">
      {layers.map((layer) => (
        <div class="layer">
          <strong>{layer.title}</strong>
          <div>{layer.items.join(" → ")}</div>
        </div>
      ))}
    </div>

    <div class="prose">
      <h2>耐用边界</h2>
      <p>
        这条流水线是架构上的稳定面，而不是一张待填空的路线图：
      </p>
    </div>
    <div class="diagram">
      <pre>
Application language schema
        ↓
Language frontend / SDK
        ↓
Canonical Schema IR
        ↓
Parser → HIR + type checking
        ↓
Typed Relational IR
        ↓
Optimizer / Planner
        ↓
Executor
        ↓
Transaction boundary
        ↓
Storage</pre>
    </div>

    <div class="prose">
      <h2>当前嵌入路径</h2>
      <p>
        现在这条路径是同步的。查询语言是刻意缩小的原生子集，不声称完整 SQL 兼容。
        核心不依赖 Go、网络运行时、JSON 执行 IR，或应用特有的 Rust 结构体。
      </p>
    </div>
    <div class="diagram">
      <pre>
Rust Schema API
    ↓
SELECT / JOIN / ORDER BY / GROUP BY + typed DML
    ↓
Typed HIR
    ↓
Logical query / DML statement plan
    ↓
Scan + nested-loop join + sort + grouped aggregate
    ↓
Join / filter / sort / aggregate / projection / limit + mutation
    ↓
Heap
    ↓
Transaction lifecycle + versioned WAL
    ↓
Buffer pool (guards, pinning, dirty writeback)
    ↓
Slotted pages
    ↓
Page manager / database file</pre>
    </div>

    <div class="prose">
      <h2>Canonical Schema IR</h2>
      <p>
        <code>netbadb-schema</code> 用显式 Rust 结构保存数据库含义，独立于任何应用语言。
        一列包含稳定 <code>ColumnId</code>、名字、<code>TypeSpec</code>（物理类型 + 可选语义名）、可空性与主键元数据。
      </p>
      <p>
        <code>Schema::new</code> 是可失败的构造路径，并委托 <code>Schema::validate</code>。
        校验拒绝重复的表 / 列 ID 与名字、空名字，以及空的语义类型名。规范名是前端无关的 UTF-8 身份，相等性精确且区分大小写。
      </p>
      <p>
        每张通过校验的 <code>TableDef</code> 都有规范编码版本 1：以 <code>NBTS</code> 开头，然后是显式小端版本、表身份与按声明顺序的列。
        这些字节上的 SHA-256 就是 32 字节 <code>SchemaFingerprint</code>。Rust 枚举判别值、结构布局、<code>Debug</code> 输出或 map 迭代顺序都不参与。
      </p>

      <h2>编译阶段保持分离</h2>
      <p>第一刀查询子集遵循：</p>
    </div>
    <div class="diagram">
      <pre>source → AST → resolved / type-checked HIR → logical plan → physical plan</pre>
    </div>
    <div class="prose">
      <p>
        HIR 拥有源级解析与语义类型检查。关系 IR 拥有关系含义与列出处。规划器选择顺序扫描，
        以及正确性优先的 nested-loop 来实现逻辑 INNER JOIN。执行器对存储返回的行求值类型化表达式。
      </p>
      <p>
        层与层之间使用 ID 与所有权值。它不把页、帧或元组的长寿命引用散播到规划器、执行器或目录里。
      </p>

      <h2>依赖方向</h2>
      <p>
        图中 <code>A → B</code> 表示 A 依赖 B。下层不得依赖上层策略。尤其是存储不得依赖规划器或执行器，
        执行器不得依赖 SDK。
      </p>
    </div>

    <div class="crate-grid">
      {
        crates.map((crate) => (
          <article class="crate">
            <h3>{crate.name}</h3>
            <p>{crate.role}</p>
            <div class="deps">{crate.path}</div>
          </article>
        ))
      }
    </div>

    <div class="prose">
      <h2>跨语言策略</h2>
      <p>Go 不再被当作数据库实现语言。预定的支持边界是：</p>
    </div>
    <div class="diagram">
      <pre>
Rust: 原生核心与嵌入式 SDK
Go:   生成 SDK 与 NetbaDB 协议客户端</pre>
    </div>
    <div class="prose">
      <p>
        <code>sdk/go</code> 目前只记录边界。起始仓库里没有 Go 运行时实现，也没有协议线格式，因此站点也不假装它们已经完成。
        未来的 <code>netbadbd</code> 必须先让协议版本化且语言中立，再围绕它生成 Go 客户端。
      </p>
    </div>

    <div class="callout note">
      <h3>决策顺序</h3>
      <p class="muted">
        正确性 &gt; 显式不变量 &gt; 类型安全 &gt; 架构清晰 &gt; 可测试性 &gt; 可观测性 &gt; 测过的性能 &gt; 便利。
        优先最小且贯通的垂直切片。不要为了让架构图看起来完整而添加空 crate、投机 trait 层次或占位 API。
      </p>
    </div>
  </div>
</BaseLayout>
